# LiteGS 项目创新点与研究计划

**文档生成时间**: 2026-03-26  
**基于**: 项目文档、代码注释、配置文件分析
**状态**: 备用，暂不执行

---

## 📋 目录

1. [已完成的主要创新点](#1-已完成的主要创新点)
2. [正在进行的研究方向](#2-正在进行的研究方向)
3. [计划中的未来工作](#3-计划中的未来工作)
4. [技术优势与特色](#4-技术优势与特色)
5. [实验验证计划](#5-实验验证计划)

---

## 1. 已完成的主要创新点

### 1.1 长记忆系统 (Long-term Memory System) ⭐

**状态**: ✅ 已完成 (v1.0.0)

**创新点**:
- **分层记忆架构**: 短期记忆 → 长期记忆 → 工作记忆三层设计
- **智能检索机制**: 混合检索策略（相似度 40% + 时间 30% + 重要性 30%）
- **遗忘曲线实现**: 艾宾浩斯遗忘曲线自动管理记忆保留率
- **记忆巩固机制**: 定期整理、合并相似记忆、强化重要记忆
- **优雅降级设计**: LanceDB 向量数据库不可用时自动切换 JSON 存储

**技术实现**:
```python
# 三层记忆架构
ShortTermMemory  →  容量限制 (默认 10), 会话级
LongTermMemory   →  持久化存储，向量数据库
WorkingMemory    →  当前推理过程跟踪
```

**文件**: 
- [`litegs/memory/`](file:///e:/Code/LiteGS/litegs/memory/) - 完整实现
- [`MEMORY_IMPLEMENTATION.md`](file:///e:/Code/LiteGS/MEMORY_IMPLEMENTATION.md) - 实施报告
- [`LONG_TERM_MEMORY.md`](file:///e:/Code/LiteGS/LONG_TERM_MEMORY.md) - 设计规范

**应用价值**:
- 记录调试经验和解决方案
- 存储性能优化技巧
- 跟踪项目上下文和决策
- 代码开发助手和性能优化顾问

---

### 1.2 TDD 测试驱动开发流程 ⭐

**状态**: ✅ 已完成

**创新点**:
- **完整的测试框架**: pytest + fixtures + 标记系统
- **自动化测试运行**: WSL2 环境下的测试脚本
- **测试驱动开发工作流**: 严格遵循 Red-Green-Refactor 循环
- **多维度测试覆盖**: 单元测试 + 集成测试 + 性能测试

**技术实现**:
```ini
# pytest 配置
[pytest]
testpaths = tests
markers =
    slow: marks tests as slow
    cuda: marks tests requiring CUDA
```

**文件**:
- [`tests/`](file:///e:/Code/LiteGS/tests/) - 测试目录
- [`TDD_WORKFLOW.md`](file:///e:/Code/LiteGS/TDD_WORKFLOW.md) - 工作流程
- [`TDD_IMPLEMENTATION_REPORT.md`](file:///e:/Code/LiteGS/TDD_IMPLEMENTATION_REPORT.md) - 实施报告

**成果**:
- 24 个单元测试用例
- 成功发现维度不匹配 bug
- 保证代码质量和可维护性

---

### 1.3 CUDA 11/12 兼容性设计

**状态**: ✅ 已完成

**创新点**:
- **向后兼容层**: CUDA 12 专用函数在 CUDA 11 中的手动实现
- **优雅降级**: 新版本特性不可用时的备用方案
- **跨版本支持**: 同时支持 CUDA 11 和 CUDA 12

**技术实现**:
```cpp
// CUDA 11 兼容层
#if __CUDA_VERSION__ < 12000
__device__ __forceinline__ unsigned int hgt2_mask(half2 a, half2 b) {
    float2 a_f = __half22float2(a);
    float2 b_f = __half22float2(b);
    unsigned int mask_x = (a_f.x > b_f.x) ? 0xFFFF : 0;
    unsigned int mask_y = (a_f.y > b_f.y) ? 0xFFFF : 0;
    return (mask_y << 16) | mask_x;
}
#define __hgt2_mask(a, b) hgt2_mask(a, b)
#endif
```

**文件**:
- [`raster.cu`](file:///e:/Code/LiteGS/litegs/submodules/gaussian_raster/raster.cu#L12-L39) - 兼容层实现

**价值**:
- 扩大硬件兼容性
- 降低用户环境配置难度
- 提高代码可移植性

---

### 1.4 Binning 模块优化

**状态**: ✅ 已完成

**创新点**:
- **统一 API 设计**: script 版本和 fused 版本接口一致
- **维度优化**: 正确的 tensor 维度变换
- **性能优化**: fused CUDA kernel 实现

**技术实现**:
```python
# 维度变换优化
eigen_val_perm = eigen_val.permute(0, 2, 1)  # [batch, 2, num_points] -> [batch, num_points, 2]
eigen_vec_perm = eigen_vec.permute(0, 3, 1, 2)  # [batch, 2, 2, num_points] -> [batch, num_points, 2, 2]
```

**文件**:
- [`litegs/utils/wrapper.py`](file:///e:/Code/LiteGS/litegs/utils/wrapper.py) - Binning 实现
- [`litegs/render/__init__.py`](file:///e:/Code/LiteGS/litegs/render/__init__.py) - 渲染集成

**性能**:
- bonsai 场景训练：9.86 秒（1000 次迭代）
- 206K primitives 处理
- 49MB 输出文件

---

## 2. 正在进行的研究方向

### 2.1 自适应密度控制 (Adaptive Densification)

**状态**: 🔄 进行中

**研究内容**:
- 基于场景复杂度的自适应 primitives 数量控制
- 动态 densification 策略
- 剪枝优化（culling）

**实验配置**:
```python
# ablations_densify.py 中的实验设计
ablation_configs_list = [
    "",  # baseline
    " --densification_interval 1 --opacity_reset_interval 20 ..."  # ADC
]
```

**目标场景**:
- Mip-NeRF360 (indoor + outdoor)
- TanksAndTemples
- DeepBlending

**预期指标**:
- 训练速度提升
- 渲染质量保持或提升
- primitives 数量减少

---

### 2.2 剪枝优化 (Pruning/Culling)

**状态**: 🔄 进行中

**研究内容**:
- 基于重要性的 primitives 剪枝
- 可见性检测优化
- 内存占用优化

**实验脚本**:
- [`scripts/ablations_culling.py`](file:///e:/Code/LiteGS/scripts/ablations_culling.py)

**目标 primitives 数量**:
```python
target_primitives = {
    "bicycle": 5987095,
    "garden": 5728191,
    "kitchen": 1803735,
    "bonsai": 1252367,
    # ...
}
```

**评估指标**:
- PSNR (Peak Signal-to-Noise Ratio)
- SSIM (Structural Similarity)
- LPIPS (Learned Perceptual Image Patch Similarity)
- 训练时间
- 内存占用

---

### 2.3 激进训练策略 (Aggressive Training)

**状态**: 🔄 进行中

**研究内容**:
- 快速收敛策略
- 减少迭代次数
- 学习率调度优化

**实验配置**:
```python
fast_config = "--iterations 10000 --position_lr_max_steps 10000 --position_lr_final 0.000016 --densification_interval 2"
```

**对比实验**:
- Baseline: 30000 次迭代
- Aggressive: 10000 次迭代
- 重复实验：3 次

**预期成果**:
- 训练速度提升 3 倍
- 质量损失控制在可接受范围

---

## 3. 计划中的未来工作

### 3.1 记忆系统增强

**计划时间**: 下一步

**研究方向**:
1. **自动记忆提取**
   - 集成 Mem0 自动提取事实
   - 减少手动存储负担
   - 80% token  reduction

2. **云同步**
   - SuperMemory API 集成
   - 跨设备记忆同步
   - 云端备份

3. **Git 集成**
   - git-notes 决策存储
   - 分支感知记忆
   - 版本控制

4. **可视化界面**
   - 记忆图谱可视化
   - 时间线浏览
   - 交互式检索

5. **智能推荐**
   - 基于上下文的主动推荐
   - 相似问题自动匹配
   - 预测性记忆加载

---

### 3.2 训练流程优化

**计划时间**: 短期

**研究方向**:
1. **完整场景评估**
   - 30000 次迭代完整训练
   - 达到论文指标质量
   - 全面性能评估

2. **多场景对比**
   - 所有标准数据集测试
   - 跨场景泛化能力
   - 鲁棒性分析

3. **超参数优化**
   - 学习率调度搜索
   - 密度控制参数调优
   - 损失权重平衡

---

### 3.3 性能优化

**计划时间**: 中期

**研究方向**:
1. **CUDA kernel 优化**
   - 更高效的 fused kernel
   - 内存访问模式优化
   - 并行度提升

2. **多 GPU 支持**
   - 数据并行
   - 模型并行
   - 混合并行策略

3. **内存优化**
   - 梯度检查点
   - 混合精度训练
   - 显存占用优化

---

### 3.4 质量提升

**计划时间**: 中长期

**研究方向**:
1. **反照率优化**
   - 更好的初始化策略
   - 材质建模
   - 光照分离

2. **几何一致性**
   - 多视图几何约束
   - 深度监督
   - 法线优化

3. **时间一致性**
   - 动态场景支持
   - 时间插值
   - 运动模糊

---

## 4. 技术优势与特色

### 4.1 架构设计优势

| 特性 | 传统方法 | LiteGS | 优势 |
|------|---------|--------|------|
| 记忆管理 | 无 | 三层架构 | 上下文感知 |
| 测试覆盖 | 低 | 完整 TDD | 高质量保证 |
| 兼容性 | 单一版本 | CUDA 11/12 | 广泛支持 |
| API 设计 | 不统一 | 一致接口 | 易用性强 |

### 4.2 性能优势

**训练速度**:
- bonsai: 9.86 秒 (1000 次迭代)
- 激进策略：预计 3 倍提升

**内存效率**:
- 动态密度控制
- 智能剪枝
- 显存优化

**代码质量**:
- 24 个单元测试
- TDD 保证
- 易于维护

### 4.3 创新特色

1. **AI 辅助开发**
   - 长记忆系统
   - 智能检索
   - 上下文感知

2. **工程化实践**
   - TDD 流程
   - 自动化测试
   - CI/CD 集成

3. **用户友好**
   - 优雅降级
   - 跨平台支持
   - 详细文档

---

## 5. 实验验证计划

### 5.1 基准测试

**数据集**:
- Mip-NeRF360 (9 场景)
- TanksAndTemples (2 场景)
- DeepBlending (2 场景)

**指标**:
- PSNR (越高越好)
- SSIM (越高越好)
- LPIPS (越低越好)
- 训练时间 (越短越好)
- 内存占用 (越少越好)

### 5.2 消融实验

**实验设计**:

1. **剪枝消融**
   - Baseline (无剪枝)
   - 不同剪枝阈值
   - 不同剪枝策略

2. **密度控制消融**
   - 无 densification
   - 不同 densification interval
   - 不同 opacity reset 策略

3. **训练策略消融**
   - 30000 次迭代 (baseline)
   - 10000 次迭代 (aggressive)
   - 不同学习率调度

### 5.3 对比实验

**对比方法**:
- 3D Gaussian Splatting (原始)
- 其他 NeRF 变体
- 传统 MVS 方法

**评估协议**:
- 相同训练数据
- 相同评估指标
- 多次实验取平均

### 5.4 时间线

| 阶段 | 时间 | 目标 |
|------|------|------|
| 阶段 1 | 第 1-2 周 | 完成 baseline 测试 |
| 阶段 2 | 第 3-4 周 | 消融实验完成 |
| 阶段 3 | 第 5-6 周 | 对比实验完成 |
| 阶段 4 | 第 7-8 周 | 论文撰写 |

---

## 6. 总结

### 已完成成果

✅ **长记忆系统**: 完整的分层记忆管理  
✅ **TDD 流程**: 测试驱动开发实践  
✅ **CUDA 兼容性**: 跨版本支持  
✅ **Binning 优化**: 性能和质量提升  

### 正在进行

🔄 **自适应密度控制**: 实验进行中  
🔄 **剪枝优化**: 消融实验  
🔄 **激进训练**: 快速收敛策略  

### 未来计划

📅 **记忆系统增强**: 5 个方向  
📅 **训练流程优化**: 3 个方面  
📅 **性能优化**: 3 个层次  
📅 **质量提升**: 3 个维度  

### 核心竞争力

1. **AI 辅助**: 独特的记忆系统
2. **工程质量**: TDD 保证
3. **性能优异**: 快速训练
4. **易于使用**: 良好文档和兼容性

---

**文档维护**: 项目团队  
**最后更新**: 2026-03-26  
**版本**: 1.0
