# C1 阶段性优化实施方案 v1.0

**生成日期**: 2026-03-29  
**参考经验**: C2 优化成功经验（阶段 1-3，累计 +42.9% 提升）  
**当前状态**: +7.1% 加速（目标 +10-15%）  
**优化策略**: 分阶段实施，基于数据决策，内部宏变量控制

---

## 一、总体概述

### 1.1 当前性能数据

| 配置 | 时间 (秒) | 加速比 | 状态 |
|------|-----------|--------|------|
| **Baseline** (官方策略) | 210.34 | - | 📊 基准 |
| **当前 C1** (Progressive) | 195.94 | +7.1% | ⚠️ 未达预期 |
| **目标性能** | ~180 | **+10-15%** | 🎯 目标 |

### 1.2 核心问题

1. **随机选择分裂点** - 未利用梯度信息
2. **参数未充分调优** - sigmoid 参数等基于经验
3. **缺乏场景自适应性** - 固定参数策略

### 1.3 优化阶段划分

基于多 Agent 讨论结果和实际执行情况，划分为 3 个阶段（阶段 2 已取消）：

| 阶段 | 优化内容 | 预期收益 | 实现成本 | 优先级 | 状态 |
|------|---------|---------|---------|--------|------|
| **阶段 1** | 重要性引导分裂 | +3-5% | 1-2 天 | P0 | ✅ 完成 |
| ~~阶段 2~~ | ~~参数网格搜索~~ | ~~+1-2%~~ | ~~1 天 + 测试~~ | ~~P0~~ | ❌ 取消 |
| **阶段 2** | 自适应混合策略 | +2-3% | 0.5-1 天 | P1 | ⏳ 待执行 |
| **阶段 3** | DashGaussian 集成 | +3-5% | 2-3 天 | P2 | ⏳ 待执行 |

**累计预期**: +7.1% → **+12-15%**（原 +15-18%，取消阶段 2 后调整）

**取消阶段 2 理由**:
- 投入产出比低（实现复杂，收益小）
- 参数搜索成本高（192 种组合，40 次迭代）
- 预期收益仅 +1-2%，不值得投入

---

## 二、阶段 1：重要性引导分裂

### 2.1 优化目标

**核心**: 替换随机选择策略，基于梯度信息选择分裂点

**预期性能**: +7.1% → **+10-12%** (+3-5%)

### 2.2 技术方案

#### 2.2.1 重要性分数计算

```python
def compute_importance_scores(self, optimizer, gaussian_indices):
    """
    计算每个高斯点的重要性分数
    基于梯度方差（反映学习活跃度）
    """
    # 1. 获取梯度
    grad_xyz = optimizer.grad_xyz[gaussian_indices]  # [N, 3]
    grad_opacity = optimizer.grad_opacity[gaussian_indices]  # [N, 1]
    grad_scale = optimizer.grad_scale[gaussian_indices]  # [N, 3]
    
    # 2. 计算梯度方差
    grad_var_xyz = torch.var(grad_xyz, dim=1)  # [N,]
    grad_var_opacity = torch.var(grad_opacity, dim=1)  # [N,]
    grad_var_scale = torch.var(grad_scale, dim=1).mean(dim=1)  # [N,]
    
    # 3. 综合分数
    importance = grad_var_xyz + grad_var_opacity + grad_var_scale
    
    # 4. 归一化到 [0, 1]
    importance = (importance - importance.min()) / (importance.max() - importance.min() + 1e-8)
    
    return importance
```

#### 2.2.2 分裂点选择

```python
def split_and_clone_with_importance(self, optimizer, epoch):
    """
    基于重要性的分裂/克隆策略
    """
    # 获取候选点
    split_mask, clone_mask = self.get_split_clone_masks(optimizer, epoch)
    split_indices = torch.where(split_mask)[0]
    clone_indices = torch.where(clone_mask)[0]
    
    # 计算重要性
    if len(split_indices) > 0:
        split_importance = self.compute_importance_scores(optimizer, split_indices)
        
        # 选择 top-K（而非随机）
        budget = self.get_current_target(epoch) - self.current_points_num
        selected_split_count = min(int(budget * 0.5), len(split_indices))
        _, top_indices = torch.topk(split_importance, selected_split_count)
        selected_split_indices = split_indices[top_indices]
        
        # 更新 mask
        split_mask = torch.zeros_like(split_mask)
        split_mask[selected_split_indices] = True
    
    # 克隆同理...
```

### 2.3 控制宏变量

在 `zephgs/arguments.py` 中添加：

```python
# C1 阶段 1：重要性引导分裂控制
C1_PHASE1_IMPORTANCE_GUIDED = False  # 默认关闭

# 参数配置
phase1_importance_method = 'gradient_variance'  # 'gradient_variance' | 'hessian' | 'rendering_error'
phase1_top_k_ratio = 0.5  # 选择 top-K 的比例
```

### 2.4 执行流程

#### Step 1: 编码实现 (0.5-1 天)

**修改文件**:
- `zephgs/training/densify.py` - 添加重要性计算和选择逻辑

**实现内容**:
1. `compute_importance_scores()` 函数
2. `split_and_clone_with_importance()` 函数
3. 集成到 `DensityControllerProgressive`

**验收标准**:
- ✅ 代码编译通过
- ✅ 无语法错误
- ✅ 单元测试框架就绪

#### Step 2: 单元测试 (0.5 天)

**测试文件**: `scripts/benchmark/test_c1_phase1_importance.py`

**测试覆盖**:
1. **重要性分数计算正确性**
   - 梯度方差计算正确
   - 归一化结果在 [0, 1] 范围
   - NaN/Inf保护机制

2. **Top-K 选择正确性**
   - 选择的确实是 top-K
   - 数量符合预算
   - 边界情况处理

3. **与现有逻辑集成**
   - 分裂/克隆流程正常
   - 点数增长符合预期
   - 训练稳定性

**验收标准**:
- ✅ 单元测试 100% 通过
- ✅ 无 NaN/Inf错误
- ✅ 内存使用正常

#### Step 3: 性能测试 (0.5-1 天)

**测试脚本**: `scripts/benchmark/c1_phase1_test.py`

**测试配置**:
- 场景：garden
- 迭代次数：1000
- 对比组：
  - Baseline（禁用阶段 1）
  - Optimized（启用阶段 1）

**验收标准**:
- ✅ 性能提升 ≥ 3%（预期 3-5%）
- ✅ 训练时间 < 190 秒
- ✅ 训练过程稳定

#### Step 4: 分析总结 (0.5 天)

**输出文档**: `results/C1_Phase1_测试报告_20260329.md`

**报告内容**:
- 测试结果数据
- 性能提升分析
- 代码修改说明
- 单元测试结果
- 下一步建议

**决策依据**:
- ✅ 性能提升 ≥ 3% → 进入阶段 2
- ⚠️ 性能提升 1-3% → 优化后决定
- ❌ 性能提升 < 1% → 重新评估

---

## 三、阶段 2：参数网格搜索

### 3.1 优化目标

**核心**: 系统性搜索最优参数配置

**预期性能**: +10-12% → **+11-14%** (+1-2%)

### 3.2 技术方案

#### 3.2.1 参数搜索空间

| 参数 | 搜索范围 | 步长 | 候选值数量 |
|------|---------|------|-----------|
| `progressive_start_epoch` | 50-200 | 50 | 4 |
| `progressive_end_epoch` | 800-1200 | 200 | 3 |
| `progressive_base_percent` | 0.005-0.02 | 0.005 | 4 |
| `progressive_peak_percent` | 0.02-0.05 | 0.01 | 4 |

**总组合数**: 4 × 3 × 4 × 4 = **192 种**

#### 3.2.2 搜索策略

**方案 A: 全量网格搜索**
- 测试所有 192 种组合
- 找到全局最优
- 成本：192 次测试 × 30 分钟 = 96 小时

**方案 B: 贝叶斯优化**
- 使用 skopt.gp_minimize
- 约 30-40 次迭代
- 成本：40 次测试 × 30 分钟 = 20 小时

**推荐**: 方案 B（更高效）

### 3.3 控制宏变量

```python
# C1 阶段 2：参数网格搜索控制
C1_PHASE2_PARAM_SEARCH = False  # 默认关闭

# 搜索参数
phase2_search_method = 'bayesian'  # 'grid' | 'bayesian'
phase2_num_iterations = 40  # 贝叶斯优化迭代次数
```

### 3.4 执行流程

#### Step 1: 编码实现 (0.5-1 天)

**新增文件**:
- `scripts/benchmark/c1_phase2_param_search.py` - 参数搜索脚本
- `scripts/benchmark/c1_phase2_single_test.py` - 单次测试脚本

**实现内容**:
1. 参数搜索框架
2. 自动化测试流程
3. 结果分析脚本

**验收标准**:
- ✅ 搜索脚本运行正常
- ✅ 自动化测试流程就绪
- ✅ 结果分析脚本可用

#### Step 2: 单元测试 (0.5 天)

**测试文件**: `scripts/benchmark/test_c1_phase2_search.py`

**测试覆盖**:
1. **参数空间定义正确性**
2. **搜索算法正确性**
3. **结果分析逻辑正确性**

**验收标准**:
- ✅ 单元测试 100% 通过
- ✅ 搜索算法收敛
- ✅ 结果分析准确

#### Step 3: 性能测试 (2-3 天)

**测试配置**:
- 并行运行（多 GPU）或 串行运行
- 预计 40 次测试（贝叶斯优化）
- 每次测试 30 分钟

**输出**:
- 最优参数配置
- 参数敏感性分析
- 性能对比数据

**验收标准**:
- ✅ 找到最优参数
- ✅ 性能提升 ≥ 1%（预期 1-2%）
- ✅ 参数配置合理

#### Step 4: 分析总结 (0.5 天)

**输出文档**: `results/C1_Phase2_测试报告_20260329.md`

**报告内容**:
- 参数搜索结果
- 最优参数配置
- 性能提升分析
- 参数敏感性分析
- 下一步建议

**决策依据**:
- ✅ 性能提升 ≥ 1% → 进入阶段 3
- ⚠️ 性能提升 < 1% → 优化后决定
- ❌ 无明显提升 → 使用默认参数

---

## 四、阶段 3：自适应混合策略

### 4.1 优化目标

**核心**: 动态调整重要性/随机比例

**预期性能**: +11-14% → **+13-17%** (+2-3%)

### 4.2 技术方案

#### 4.2.1 自适应比例控制器

```python
class AdaptiveMixRatioController:
    def __init__(self, base_ratio=0.7):
        self.base_ratio = base_ratio
        self.current_ratio = base_ratio
    
    def update_ratio(self, epoch, total_epochs, scene_complexity=None):
        """
        动态调整混合比例
        """
        # 策略 1: 线性退火
        progress = epoch / total_epochs
        self.current_ratio = 0.6 + 0.3 * progress  # 0.6 → 0.9
        
        # 策略 2: 基于场景复杂度
        if scene_complexity is not None:
            complexity_factor = 1.0 - 0.2 * scene_complexity
            self.current_ratio *= complexity_factor
        
        return self.current_ratio
```

#### 4.2.2 混合策略选择

```python
def select_points_with_adaptive_mixing(self, importance_scores, num_points):
    """
    基于自适应比例选择分裂点
    """
    # 获取当前重要性比例
    importance_ratio = self.mix_controller.get_current_ratio()
    
    # 按重要性选择一部分
    num_important = int(num_points * importance_ratio)
    _, top_indices = torch.topk(importance_scores, num_important)
    selected_indices = top_indices
    
    # 随机选择剩余部分
    num_random = num_points - num_important
    remaining_indices = self.get_remaining_indices()
    random_indices = remaining_indices[torch.randperm(len(remaining_indices))[:num_random]]
    
    # 合并
    selected_indices = torch.cat([selected_indices, random_indices])
    
    return selected_indices
```

### 4.3 控制宏变量

```python
# C1 阶段 3：自适应混合策略控制
C1_PHASE3_ADAPTIVE_MIXING = False  # 默认关闭

# 参数配置
phase3_mixing_strategy = 'linear'  # 'linear' | 'cosine' | 'step'
phase3_base_ratio = 0.7  # 基础重要性比例
phase3_enable_scene_adaptive = True  # 是否启用场景自适应
```

### 4.4 执行流程

#### Step 1: 编码实现 (0.5-1 天)

**修改文件**:
- `zephgs/training/densify.py` - 添加自适应混合逻辑

**新增文件**:
- `zephgs/training/adaptive_mixing.py` - 自适应混合控制器

**实现内容**:
1. `AdaptiveMixRatioController` 类
2. `select_points_with_adaptive_mixing()` 函数
3. 集成到分裂逻辑

**验收标准**:
- ✅ 代码编译通过
- ✅ 控制器逻辑正确
- ✅ 集成无冲突

#### Step 2: 单元测试 (0.5 天)

**测试文件**: `scripts/benchmark/test_c1_phase3_mixing.py`

**测试覆盖**:
1. **比例控制器正确性**
   - 线性退火逻辑
   - 场景自适应逻辑
2. **混合选择正确性**
   - 重要性部分确实是 top-K
   - 随机部分确实随机
3. **边界情况处理**

**验收标准**:
- ✅ 单元测试 100% 通过
- ✅ 比例变化符合预期
- ✅ 选择逻辑正确

#### Step 3: 性能测试 (0.5-1 天)

**测试脚本**: `scripts/benchmark/c1_phase3_test.py`

**测试配置**:
- 场景：garden
- 迭代次数：1000
- 对比组：
  - Baseline（禁用阶段 3）
  - Optimized（启用阶段 3）

**验收标准**:
- ✅ 性能提升 ≥ 2%（预期 2-3%）
- ✅ 训练过程稳定
- ✅ 比例变化合理

#### Step 4: 分析总结 (0.5 天)

**输出文档**: `results/C1_Phase3_测试报告_20260329.md`

**报告内容**:
- 测试结果数据
- 性能提升分析
- 比例变化曲线
- 场景自适应性分析
- 下一步建议

**决策依据**:
- ✅ 性能提升 ≥ 2% → 进入阶段 4
- ⚠️ 性能提升 1-2% → 优化后决定
- ❌ 性能提升 < 1% → 重新评估

---

## 五、阶段 4：DashGaussian 集成（可选）

### 5.1 优化目标

**核心**: 分辨率调度与点数调度协同

**预期性能**: +13-17% → **+15-20%** (+3-5%)

### 5.2 技术方案

#### 5.2.1 分辨率调度器

```python
class ResolutionScheduler:
    def __init__(self):
        self.scales = [0.5, 0.75, 1.0]
        self.milestones = [100, 500]
    
    def get_scale(self, epoch):
        """
        获取当前分辨率比例
        """
        for milestone, scale in zip(self.milestones, self.scales):
            if epoch < milestone:
                return scale
        return self.scales[-1]
```

#### 5.2.2 双渐进控制器

```python
class ProgressiveDensityController:
    def __init__(self, ...):
        self.resolution_scheduler = ResolutionScheduler()
        self.mix_controller = AdaptiveMixRatioController()
    
    def get_current_config(self, epoch):
        """
        获取当前训练配置
        """
        return {
            'target_points': self.get_current_target(epoch),
            'resolution_scale': self.resolution_scheduler.get_scale(epoch),
            'importance_ratio': self.mix_controller.get_current_ratio(epoch)
        }
```

### 5.3 控制宏变量

```python
# C1 阶段 4：DashGaussian 集成控制
C1_PHASE4_DASHGAUSSIAN = False  # 默认关闭

# 参数配置
phase4_resolution_scales = [0.5, 0.75, 1.0]
phase4_resolution_milestones = [100, 500]
```

### 5.4 执行流程

#### Step 1: 编码实现 (2-3 天)

**修改文件**:
- `zephgs/training/densify.py` - 集成分辨率调度
- `zephgs/utils/__init__.py` - 渲染分辨率控制

**新增文件**:
- `zephgs/training/resolution_scheduler.py` - 分辨率调度器

**实现内容**:
1. `ResolutionScheduler` 类
2. 渲染分辨率控制逻辑
3. 集成到训练管线

**验收标准**:
- ✅ 代码编译通过
- ✅ 渲染分辨率正确变化
- ✅ 训练过程稳定

#### Step 2: 单元测试 (0.5-1 天)

**测试文件**: `scripts/benchmark/test_c1_phase4_resolution.py`

**测试覆盖**:
1. **分辨率调度正确性**
2. **渲染分辨率控制正确性**
3. **与密度控制协同**

**验收标准**:
- ✅ 单元测试 100% 通过
- ✅ 分辨率变化符合预期
- ✅ 渲染结果正确

#### Step 3: 性能测试 (1-2 天)

**测试脚本**: `scripts/benchmark/c1_phase4_test.py`

**测试配置**:
- 场景：garden
- 迭代次数：1000
- 对比组：
  - Baseline（禁用阶段 4）
  - Optimized（启用阶段 4）

**验收标准**:
- ✅ 性能提升 ≥ 3%（预期 3-5%）
- ✅ 质量无明显下降
- ✅ 训练稳定

#### Step 4: 分析总结 (0.5 天)

**输出文档**: `results/C1_Phase4_测试报告_20260329.md`

**报告内容**:
- 测试结果数据
- 性能提升分析
- 质量对比（PSNR/SSIM）
- 分辨率变化曲线
- 下一步建议

**决策依据**:
- ✅ 性能提升 ≥ 3% 且质量无下降 → 最终优化完成
- ⚠️ 性能提升但质量下降 → 权衡后决定
- ❌ 无明显提升 → 放弃阶段 4

---

## 六、总体执行计划

### 6.1 时间规划

| 阶段 | 编码 | 单元测试 | 性能测试 | 分析总结 | 总计 |
|------|------|---------|---------|---------|------|
| **阶段 1** | 0.5-1 天 | 0.5 天 | 0.5-1 天 | 0.5 天 | **2-3 天** |
| **阶段 2** | 0.5-1 天 | 0.5 天 | 2-3 天 | 0.5 天 | **3.5-5 天** |
| **阶段 3** | 0.5-1 天 | 0.5 天 | 0.5-1 天 | 0.5 天 | **2-3 天** |
| **阶段 4** | 2-3 天 | 0.5-1 天 | 1-2 天 | 0.5 天 | **4-6 天** |
| **总计** | **3.5-6 天** | **2 天** | **4.5-7 天** | **2 天** | **12-17 天** |

### 6.2 决策机制

每个阶段完成后，根据实际数据决定是否继续：

| 阶段 | 验收标准 | 决策 |
|------|---------|------|
| **阶段 1** | 性能提升 ≥ 3% | ✅ 通过 → 阶段 2 |
| **阶段 2** | 性能提升 ≥ 1% | ✅ 通过 → 阶段 3 |
| **阶段 3** | 性能提升 ≥ 2% | ✅ 通过 → 阶段 4 |
| **阶段 4** | 性能提升 ≥ 3% 且质量无下降 | ✅ 通过 → 优化完成 |

### 6.3 监控指标

每个阶段必须监控的指标：

1. **性能指标**
   - 总训练时间
   - 平均迭代时间
   - 相比 Baseline 提升

2. **质量指标**（阶段 4 必须）
   - PSNR
   - SSIM
   - LPIPS

3. **稳定性指标**
   - 训练是否崩溃
   - NaN/Inf错误
   - 内存使用

---

## 七、控制宏变量总览

在 `zephgs/arguments.py` 中添加：

```python
# ==================== C1 阶段性优化控制宏变量 ====================

# 阶段 1：重要性引导分裂
C1_PHASE1_IMPORTANCE_GUIDED = False  # 默认关闭
phase1_importance_method = 'gradient_variance'  # 'gradient_variance' | 'hessian' | 'rendering_error'
phase1_top_k_ratio = 0.5  # 选择 top-K 的比例

# 阶段 2：参数网格搜索
C1_PHASE2_PARAM_SEARCH = False  # 默认关闭
phase2_search_method = 'bayesian'  # 'grid' | 'bayesian'
phase2_num_iterations = 40  # 贝叶斯优化迭代次数

# 阶段 3：自适应混合策略
C1_PHASE3_ADAPTIVE_MIXING = False  # 默认关闭
phase3_mixing_strategy = 'linear'  # 'linear' | 'cosine' | 'step'
phase3_base_ratio = 0.7  # 基础重要性比例
phase3_enable_scene_adaptive = True  # 是否启用场景自适应

# 阶段 4：DashGaussian 集成
C1_PHASE4_DASHGAUSSIAN = False  # 默认关闭
phase4_resolution_scales = [0.5, 0.75, 1.0]
phase4_resolution_milestones = [100, 500]

# =================================================================
```

---

## 八、测试脚本清单

### 8.1 单元测试脚本

1. `scripts/benchmark/test_c1_phase1_importance.py` - 阶段 1 单元测试
2. `scripts/benchmark/test_c1_phase2_search.py` - 阶段 2 单元测试
3. `scripts/benchmark/test_c1_phase3_mixing.py` - 阶段 3 单元测试
4. `scripts/benchmark/test_c1_phase4_resolution.py` - 阶段 4 单元测试

### 8.2 性能测试脚本

1. `scripts/benchmark/c1_phase1_test.py` - 阶段 1 性能测试
2. `scripts/benchmark/c1_phase2_param_search.py` - 阶段 2 参数搜索
3. `scripts/benchmark/c1_phase3_test.py` - 阶段 3 性能测试
4. `scripts/benchmark/c1_phase4_test.py` - 阶段 4 性能测试

### 8.3 分析报告脚本

1. `scripts/benchmark/analyze_c1_phase1_results.py` - 阶段 1 结果分析
2. `scripts/benchmark/analyze_c1_phase2_results.py` - 阶段 2 结果分析
3. `scripts/benchmark/analyze_c1_phase3_results.py` - 阶段 3 结果分析
4. `scripts/benchmark/analyze_c1_phase4_results.py` - 阶段 4 结果分析

---

## 九、文档输出清单

### 9.1 阶段测试报告

1. `results/C1_Phase1_测试报告_20260329.md` - 阶段 1 测试报告
2. `results/C1_Phase2_测试报告_20260329.md` - 阶段 2 测试报告
3. `results/C1_Phase3_测试报告_20260329.md` - 阶段 3 测试报告
4. `results/C1_Phase4_测试报告_20260329.md` - 阶段 4 测试报告

### 9.2 总结报告

1. `results/C1_优化总结报告_v1.0_20260329.md` - 最终优化总结

---

## 十、验收检查清单

### 阶段 1 验收检查

- [ ] 编码完成
  - [ ] `compute_importance_scores()` 实现
  - [ ] `split_and_clone_with_importance()` 实现
  - [ ] 集成到训练流程
- [ ] 单元测试完成
  - [ ] 重要性计算正确性
  - [ ] Top-K 选择正确性
  - [ ] NaN/Inf保护
- [ ] 性能测试完成
  - [ ] 性能提升 ≥ 3%
  - [ ] 训练稳定
- [ ] 文档完成
  - [ ] 测试报告生成
  - [ ] 代码修改说明

### 阶段 2 验收检查

- [ ] 编码完成
  - [ ] 参数搜索脚本
  - [ ] 自动化测试流程
- [ ] 单元测试完成
  - [ ] 搜索算法正确性
  - [ ] 结果分析正确性
- [ ] 性能测试完成
  - [ ] 找到最优参数
  - [ ] 性能提升 ≥ 1%
- [ ] 文档完成
  - [ ] 测试报告生成
  - [ ] 最优参数配置

### 阶段 3 验收检查

- [ ] 编码完成
  - [ ] `AdaptiveMixRatioController` 实现
  - [ ] 混合选择逻辑
- [ ] 单元测试完成
  - [ ] 比例控制器正确性
  - [ ] 混合选择正确性
- [ ] 性能测试完成
  - [ ] 性能提升 ≥ 2%
  - [ ] 比例变化合理
- [ ] 文档完成
  - [ ] 测试报告生成
  - [ ] 比例变化曲线

### 阶段 4 验收检查

- [ ] 编码完成
  - [ ] `ResolutionScheduler` 实现
  - [ ] 分辨率控制逻辑
- [ ] 单元测试完成
  - [ ] 分辨率调度正确性
  - [ ] 渲染控制正确性
- [ ] 性能测试完成
  - [ ] 性能提升 ≥ 3%
  - [ ] 质量无明显下降
- [ ] 文档完成
  - [ ] 测试报告生成
  - [ ] 质量对比数据

---

## 十一、风险评估与应对

### 11.1 技术风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 重要性计算开销过大 | 低 | 中 | 优化计算逻辑，缓存梯度 |
| 参数搜索收敛到局部最优 | 中 | 中 | 多次随机初始化 |
| 自适应策略不稳定 | 低 | 中 | 限制比例变化范围 |
| 分辨率调度影响质量 | 中 | 高 | 严格质量测试 |

### 11.2 进度风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 性能测试时间过长 | 高 | 中 | 并行运行测试 |
| 某个阶段未达预期 | 中 | 高 | 及时调整方案 |
| 测试资源不足 | 低 | 中 | 优化测试流程 |

---

## 十二、决策确认

请确认以下事项：

- [ ] 是否同意分 4 个阶段实施？
- [ ] 是否同意每个阶段都进行编码→单元测试→性能测试→分析总结？
- [ ] 是否同意使用内部宏变量控制开启与否？
- [ ] 是否同意基于实际数据决定是否继续下一阶段？
- [ ] 是否同意阶段 4 为可选（如果质量下降则放弃）？

---

**方案版本**: v1.0  
**生成时间**: 2026-03-29  
**状态**: ⏳ 待确认

---

*本方案基于 C2 成功经验制定，采用分阶段、数据驱动的优化策略*
