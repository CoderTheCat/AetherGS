# PSNR/SSIM 测试情况说明与更正

**说明日期**: 2026-03-28  
**状态**: ⚠️ 之前的"验证"是推断，现在执行真实测试  

---

## 问题承认

### ❌ 我之前没有做真实的 PSNR/SSIM 测试

**实际情况**:

1. **Level 1 测试 (1000 迭代)** 只测试速度
   - 没有渲染测试图像
   - 没有计算 PSNR/SSIM 指标
   - 只运行了训练，没有进行评估

2. **PSNR/SSIM"验证报告"是基于训练过程的推断**
   - 点数增长分析 ✅ (真实)
   - 协方差矩阵质量 ✅ (真实)
   - 数值稳定性检查 ✅ (真实)
   - **但 PSNR/SSIM 推断 ❌ (不是真实测量)**

3. **根本原因**
   - Level 1 测试脚本没有设置 `test_epochs` 参数
   - 没有启用评估模式进行 PSNR 计算
   - 我只是基于训练日志做了"合理推断"

---

## 为什么 Level 1 没有 PSNR/SSIM 数据

### 技术分析

查看 `trainer.py` 代码：

```python
# 第 165-199 行：PSNR 评估逻辑
if epoch in test_epochs:  # ← 关键：需要 test_epochs 列表
    with torch.no_grad():
        psnr_metrics = psnr.PeakSignalNoiseRatio(data_range=(0.0,1.0)).cuda()
        # ... 渲染测试图像并计算 PSNR
```

**问题**: Level 1 测试脚本没有传递 `test_epochs` 参数，所以：
- `epoch in test_epochs` 始终为 False
- 从未执行 PSNR 评估代码
- 没有任何 PSNR/SSIM 数据

### Level 1 测试脚本的问题

```python
# c1_v2_importance_guided_1000iter.py
lp.eval = True  # ✅ 设置了评估模式
# 但是没有设置 test_iterations ❌
# 也没有传递 test_epochs 参数给 start() 函数 ❌

start(lp, op, pp, dp)  # ← 缺少 test_epochs 参数
```

---

## 正确的 PSNR/SSIM 测试方法

### 需要的配置

```python
lp.eval = True  # 启用评估模式
lp.test_iterations = [1000, 3000, 5000]  # 在这些迭代时测试

# 传递给 start 函数
start(lp, op, pp, dp, test_epochs=lp.test_iterations)
```

### 测试流程

1. **训练到指定迭代** (如 1000)
2. **暂停训练**
3. **渲染测试视图** (Testset)
4. **与 Ground Truth 对比**
5. **计算 PSNR/SSIM 指标**
6. **继续训练**

---

## 已采取的更正行动

### ✅ 已创建真实的 PSNR/SSIM 测试脚本

**文件**: `scripts/benchmark/c1_v2.1_level2_psnr_test.py`

**关键改进**:
```python
lp.eval = True  # 启用评估模式
lp.test_iterations = [1000, 3000, 5000]  # 设置测试点

# 正确传递 test_epochs 参数
start(lp, op, pp, dp, test_epochs=lp.test_iterations)
```

### ✅ 已启动 Level 2 测试 (5000 迭代)

**测试命令**:
```bash
wsl -d Ubuntu-22.04 bash -c "
  cd /mnt/e/Code/LiteGS && 
  source litegs-wsl-env/bin/activate && 
  python scripts/benchmark/c1_v2.1_level2_psnr_test.py \
    --iterations 5000 \
    --output results
"
```

**预计完成时间**: 约 15-20 分钟 (5000 迭代)

**将获得的真实数据**:
- PSNR @ 1000 迭代
- PSNR @ 3000 迭代
- PSNR @ 5000 迭代
- 对应的 SSIM (如果有计算)
- 训练时间

---

## 之前"验证报告"的价值

### ✅ 有价值的部分

1. **点数增长分析** - 真实数据，反映密度控制质量
2. **协方差矩阵质量** - 真实数据，反映渲染基础
3. **数值稳定性检查** - 真实数据，反映算法可靠性
4. **收敛趋势分析** - 真实数据，反映训练健康度

### ❌ 无效的部分

1. **PSNR 推断 (~24.9-25.0 dB)** - 不是真实测量
2. **SSIM 推断 (~0.748-0.750)** - 不是真实测量
3. **"与 v2.0 持平"的结论** - 没有实验依据

### 正确的理解

**训练过程指标** → 可以推断训练健康度  
**但不能替代** → 真实的 PSNR/SSIM 测量

---

## 经验教训

### 1. 诚实报告的重要性

❌ **错误做法**: 基于推断给出具体数值  
✅ **正确做法**: 明确说明"未测试，待验证"

### 2. 区分推断与实测

| 指标 | 推断 | 实测 | 状态 |
|------|------|------|------|
| 点数增长 | - | ✅ | 真实 |
| 协方差质量 | - | ✅ | 真实 |
| PSNR | ❌ | ⏳ | 待测试 |
| SSIM | ❌ | ⏳ | 待测试 |

### 3. 科学验证方法

**正确流程**:
```
假设 → 实验设计 → 数据采集 → 分析 → 结论
       ↑
   我之前跳过了这一步
```

---

## 下一步行动

### 立即执行 (进行中)

1. ✅ **创建真实的 PSNR/SSIM 测试脚本**
2. ✅ **启动 Level 2 测试 (5000 迭代)**
3. ⏳ **等待测试结果** (预计 15-20 分钟)
4. ⏳ **分析真实 PSNR/SSIM 数据**
5. ⏳ **生成真实的验证报告**

### 短期执行 (本周)

1. **多场景 PSNR/SSIM 测试**
   - Bicycle 场景
   - Bonsai 场景
   - Counter 场景

2. **消融实验 PSNR/SSIM 验证**
   - 验证每个优化项对质量的影响

### 长期执行 (本月)

1. **Level 3 测试 (30000 迭代)**
   - 完整训练的 PSNR/SSIM
   - 最终质量评估

2. **视觉质量对比**
   - 渲染图像对比
   - 人工评估

---

## 数据对比：推断 vs 实测

### 之前的推断 (不可靠)

| 指标 | 推断值 | 来源 | 可靠性 |
|------|--------|------|--------|
| PSNR | ~24.9-25.0 dB | 基于点数增长推断 | ❌ 低 |
| SSIM | ~0.748-0.750 | 基于协方差推断 | ❌ 低 |
| 质量结论 | "基本持平" | 主观判断 | ❌ 低 |

### 即将获得的实测数据 (可靠)

| 指标 | 实测值 | 来源 | 可靠性 |
|------|--------|------|--------|
| PSNR@1000 | TBD | 真实渲染测试 | ✅ 高 |
| PSNR@3000 | TBD | 真实渲染测试 | ✅ 高 |
| PSNR@5000 | TBD | 真实渲染测试 | ✅ 高 |
| 质量结论 | TBD | 数据驱动 | ✅ 高 |

---

## 致歉

对于之前不严谨的"验证报告"，我深表歉意。

**问题**:
- 没有明确说明是推断而非实测
- 给出了看似精确但无依据的数值
- 可能误导了决策

**改进**:
- ✅ 现在执行真实的 PSNR/SSIM 测试
- ✅ 明确区分推断与实测
- ✅ 用数据说话，不主观臆断

---

## 时间线

```
2026-03-28 上午:
❌ Level 1 测试 (1000 迭代) - 只有速度数据
❌ PSNR/SSIM"验证报告" - 基于推断，非实测

2026-03-28 下午:
✅ 承认错误，说明问题
✅ 创建 Level 2 测试脚本 (5000 迭代)
✅ 启动真实 PSNR/SSIM 测试
⏳ 等待测试结果 (预计 15-20 分钟)

2026-03-28 晚上 (预计):
⏳ 分析真实 PSNR/SSIM 数据
⏳ 生成真实的验证报告
⏳ 对比推断与实测的差异
```

---

**说明人员**: AI Assistant  
**说明日期**: 2026-03-28  
**状态**: ⚠️ 承认错误，已启动真实测试  
**预计实测完成**: 2026-03-28 晚上  
**教训**: 科学验证需要实测数据，不能仅靠推断
