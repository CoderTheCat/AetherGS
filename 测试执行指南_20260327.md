# C 类创新点测试执行指南

**文档版本**: 1.0  
**创建日期**: 2026-03-27  
**适用环境**: WSL2 (Ubuntu 22.04)

---

## 📋 快速开始

### 1. 环境准备

```bash
# 进入 WSL2
wsl -d Ubuntu-22.04 -u root

# 进入项目目录
cd /mnt/e/Code/LiteGS

# 激活虚拟环境
source litegs-wsl-env/bin/activate

# 验证环境
python --version
which python
```

### 2. 执行测试

#### 阶段 0: 单元测试（必须先执行）

```bash
# 运行单元测试
python -m pytest tests/unit/test_c_class_innovations.py -v

# 或者使用 Python 直接运行
python tests/unit/test_c_class_innovations.py
```

**预期输出**:
```
============================================================
C 类创新点单元测试
============================================================

测试内容:
1. 渐进式密度控制参数测试
2. FP8 混合精度训练参数测试
3. 视锥剔除增强参数测试
4. C 类创新点集成测试
============================================================

test_progressive_parameters_exist (__main__.TestProgressiveDensify)
测试渐进式密度控制参数是否存在 ... ok
test_progressive_default_values (__main__.TestProgressiveDensify)
测试渐进式密度控制默认值 ... ok
...

----------------------------------------------------------------------
Ran 10 tests in 0.002s

OK
```

**通过标准**: 所有测试通过（OK）

---

#### 阶段 1: 单点 1000 迭代测试

**前提**: 阶段 0 单元测试全部通过

##### 1.1 渐进式密度控制测试

```bash
cd /mnt/e/Code/LiteGS
source litegs-wsl-env/bin/activate

# Baseline 测试
python scripts/benchmark/progressive_densify_1000iter.py \
    --baseline \
    --output results/baseline_progressive_$(date +%Y%m%d_%H%M%S).json

# 实验组测试
python scripts/benchmark/progressive_densify_1000iter.py \
    --output results/progressive_$(date +%Y%m%d_%H%M%S).json
```

**预计时间**: ~30 分钟

##### 1.2 FP8 混合精度训练测试

```bash
# Baseline 测试
python scripts/benchmark/fp8_mixed_precision_1000iter.py \
    --baseline \
    --output results/baseline_fp8_$(date +%Y%m%d_%H%M%S).json

# 实验组测试
python scripts/benchmark/fp8_mixed_precision_1000iter.py \
    --output results/fp8_$(date +%Y%m%d_%H%M%S).json
```

**预计时间**: ~30 分钟

##### 1.3 视锥剔除增强测试

```bash
# Baseline 测试
python scripts/benchmark/frustum_culling_enhanced_1000iter.py \
    --baseline \
    --output results/baseline_culling_$(date +%Y%m%d_%H%M%S).json

# 实验组测试
python scripts/benchmark/frustum_culling_enhanced_1000iter.py \
    --output results/enhanced_culling_$(date +%Y%m%d_%H%M%S).json
```

**预计时间**: ~30 分钟

---

#### 阶段 2: 联合 1000 迭代测试

**前提**: 阶段 1 所有单点测试通过

**注意**: 需要创建联合测试脚本

```bash
# C1+C2 联合测试
python scripts/benchmark/progressive_fp8_1000iter.py \
    --output results/progressive_fp8_$(date +%Y%m%d_%H%M%S).json

# C1+C3 联合测试
python scripts/benchmark/progressive_culling_1000iter.py \
    --output results/progressive_culling_$(date +%Y%m%d_%H%M%S).json

# C2+C3 联合测试
python scripts/benchmark/fp8_culling_1000iter.py \
    --output results/fp8_culling_$(date +%Y%m%d_%H%M%S).json

# C1+C2+C3 全量联合测试
python scripts/benchmark/c_class_combined_1000iter.py \
    --output results/combined_$(date +%Y%m%d_%H%M%S).json
```

---

## 🔧 自动化测试脚本

### 使用自动化脚本（推荐）

```bash
cd /mnt/e/Code/LiteGS
source litegs-wsl-env/bin/activate
bash scripts/benchmark/run_c_class_tests_wsl2.sh
```

**特点**:
- ✅ 自动执行所有测试
- ✅ 自动生成测试报告
- ✅ 失败时自动停止后续测试
- ✅ 记录详细日志

---

## 📊 结果分析

### 查看测试结果

```bash
# 查看结果文件
ls -lh results/

# 查看测试总结
cat results/test_summary_*.txt

# 查看具体测试结果
cat results/progressive_*.json | python -m json.tool
```

### 分析测试结果

```bash
# 分析渐进式密度控制结果
python scripts/analyze/analyze_c_class_results.py \
    --baseline results/baseline_progressive_*.json \
    --experimental results/progressive_*.json \
    --innovation "progressive_densify" \
    --output results/analysis/

# 分析 FP8 混合精度结果
python scripts/analyze/analyze_c_class_results.py \
    --baseline results/baseline_fp8_*.json \
    --experimental results/fp8_*.json \
    --innovation "fp8_mixed_precision" \
    --output results/analysis/

# 分析视锥剔除增强结果
python scripts/analyze/analyze_c_class_results.py \
    --baseline results/baseline_culling_*.json \
    --experimental results/enhanced_culling_*.json \
    --innovation "enhanced_culling" \
    --output results/analysis/
```

**输出内容**:
- 📄 文本报告（`.txt`）
- 📊 对比图表（`.png`）
- 📋 JSON 结果（`.json`）

---

## ⚠️ 常见问题

### Q1: 单元测试失败

**解决方案**:
```bash
# 检查 Python 环境
which python
python --version

# 检查依赖
pip list | grep torch

# 重新安装依赖
pip install -r requirements.txt
```

### Q2: 导入错误

**错误**: `ModuleNotFoundError: No module named 'litegs_fused'`

**解决方案**:
```bash
# 复制 litegs_fused 库文件
cp /mnt/e/Code/LiteGS/litegs/submodules/gaussian_raster/litegs_fused.cpython-310-x86_64-linux-gnu.so \
   /mnt/e/Code/LiteGS/litegs-wsl-env/lib/python3.10/site-packages/

# 复制 simple_knn 库文件
mkdir -p /mnt/e/Code/LiteGS/litegs-wsl-env/lib/python3.10/site-packages/simple_knn
cp /mnt/e/Code/LiteGS/litegs/submodules/simple-knn/simple_knn/_C.cpython-310-x86_64-linux-gnu.so \
   /mnt/e/Code/LiteGS/litegs-wsl-env/lib/python3.10/site-packages/simple_knn/
```

### Q3: 测试脚本报错

**错误**: `ImportError: cannot import name 'get_default_arg'`

**解决方案**:
- 检查测试脚本是否使用 `import litegs.config`
- 使用 `litegs.config.get_default_arg()` 而非 `get_default_arg()`

### Q4: 显存不足 (OOM)

**解决方案**:
1. 关闭其他占用 GPU 的程序
2. 降低测试分辨率
3. 减少迭代次数（1000 → 500）

---

## 📝 测试记录模板

### 测试日志

**测试日期**: YYYY-MM-DD HH:MM  
**测试人员**: [姓名]  
**测试环境**: 
- GPU: [型号]
- CUDA: [版本]
- Python: [版本]

**测试结果**:
```
阶段 0: 单元测试
- 渐进式密度控制: ✅ / ❌
- FP8 混合精度：✅ / ❌
- 视锥剔除增强：✅ / ❌

阶段 1: 单点测试
- C1 Baseline 时间：XXX 秒
- C1 实验组时间：XXX 秒
- C1 提升：XX%
```

**结论**: ✅ 通过 / ❌ 失败  
**备注**: [任何问题或观察]

---

## 📅 测试时间表

| 日期 | 阶段 | 测试内容 | 预计时间 |
|------|------|----------|----------|
| 第 1 天 | 阶段 0 | 单元测试 | 10 分钟 |
| 第 1 天 | 阶段 1.1 | 渐进式密度控制 | 30 分钟 |
| 第 2 天 | 阶段 1.2 | FP8 混合精度 | 30 分钟 |
| 第 2 天 | 阶段 1.3 | 视锥剔除增强 | 30 分钟 |
| 第 3 天 | 阶段 2 | 联合测试 | 2 小时 |
| 第 4-5 天 | 阶段 3 | 5000 迭代测试 | 4 小时 |
| 第 6-10 天 | 阶段 4 | 30000 迭代测试 | 10 小时 |

---

## 🔗 相关文档

1. [`测试方案_V2.0_20260327.md`](测试方案_V2.0_20260327.md) - 完整测试方案
2. [`README_WSL2.md`](scripts/benchmark/README_WSL2.md) - WSL2 测试指南
3. [`C 类创新点实施执行总结.md`](C 类创新点实施执行总结.md) - 实施详情

---

**文档状态**: ✅ 可用  
**最后更新**: 2026-03-27  
**维护者**: [待分配]
